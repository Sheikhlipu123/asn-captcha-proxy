import type { Metadata } from 'next'
import { GeistSans } from 'geist/font/sans'
import { GeistMono } from 'geist/font/mono'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

export const metadata: Metadata = {
  title: 'asn-captcha-proxy',
  description: 'High-performance reverse proxy that protects Apache servers with ASN-based traffic filtering and CAPTCHA challenges. Automatically blocks malicious traffic using curated ASN blocklists while maintaining seamless user experience for legitimate visitors.',

}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
